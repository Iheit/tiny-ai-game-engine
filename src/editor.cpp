#define WIN32_LEAN_AND_MEAN
#include <windows.h>
#include <commdlg.h>
#include <filesystem>
#include <string>
#include <vector>
#include <fstream>
#include "engine.hpp"
#include "script.hpp"
using namespace tiny; namespace fs=std::filesystem;
static HWND gWnd,gTree,gName,gX,gY,gZ,gRX,gRY,gRZ,gSX,gSY,gSZ,gOut; static Engine g; static int sel=-1; static std::wstring project;
static int IDs=100; enum{NEWP=1,OPENP,SAVE,BUILD,PLAY,STOP,ADD_CUBE,ADD_SPHERE,ADD_PLANE,ADD_CAM,ADD_DIR,ADD_POINT,ADD_SPOT,DEL,DUP,APPLY,FOCUS};
static void out(const std::string&s){SetWindowTextA(gOut,s.c_str());}
static HWND edit(HWND p,const char*t,int x,int y,int w,int h){return CreateWindowA("EDIT",t,WS_CHILD|WS_VISIBLE|WS_BORDER|ES_AUTOHSCROLL,x,y,w,h,p,nullptr,nullptr,nullptr);} static HWND button(HWND p,const char*t,int id,int x,int y,int w){return CreateWindowA("BUTTON",t,WS_CHILD|WS_VISIBLE|BS_PUSHBUTTON,x,y,w,28,p,(HMENU)(INT_PTR)id,nullptr,nullptr);} static void label(HWND p,const char*t,int x,int y){CreateWindowA("STATIC",t,WS_CHILD|WS_VISIBLE,x,y,80,20,p,nullptr,nullptr,nullptr);}
static void refresh(){SendMessageA(gTree,LB_RESETCONTENT,0,0);for(auto&e:g.scene().entities){std::string s=e.name+"  ["+kindName(e.kind)+"]";SendMessageA(gTree,LB_ADDSTRING,0,(LPARAM)s.c_str());}if(sel>=0&&sel<(int)g.scene().entities.size()){auto&e=g.scene().entities[sel];SetWindowTextA(gName,e.name.c_str());char b[64];auto set=[&](HWND h,float v){sprintf_s(b,"%.3f",v);SetWindowTextA(h,b);};set(gX,e.transform.position.x);set(gY,e.transform.position.y);set(gZ,e.transform.position.z);set(gRX,e.transform.rotation.x);set(gRY,e.transform.rotation.y);set(gRZ,e.transform.rotation.z);set(gSX,e.transform.scale.x);set(gSY,e.transform.scale.y);set(gSZ,e.transform.scale.z);SendMessageA(gTree,LB_SETCURSEL,sel,0);}InvalidateRect(gWnd,nullptr,FALSE);}
static void save(){if(project.empty()){out("No project open.");return;}if(!writeUtf8File(project,serialize(g.scene())))out("Save failed.");else out("Saved "+narrow(project));}
static bool load(){std::string s,e;if(!readUtf8File(project,s)){out("Could not read project.");return false;}if(!deserialize(s,g.scene(),e)){out("Load error: "+e);return false;}sel=g.scene().entities.empty()?-1:0;refresh();out("Loaded "+narrow(project));return true;}
static void newProject(){char f[MAX_PATH]="MyGame.tinyproj";OPENFILENAMEA o{};o.lStructSize=sizeof(o);o.hwndOwner=gWnd;o.lpstrFile=f;o.nMaxFile=MAX_PATH;o.lpstrFilter="Tiny project\0*.tinyproj\0\0";o.Flags=OFN_OVERWRITEPROMPT;if(!GetSaveFileNameA(&o))return;project=widen(f);g=Engine{};g.initialize(gWnd,900,620);save();refresh();}
static void openProject(){char f[MAX_PATH]{};OPENFILENAMEA o{};o.lStructSize=sizeof(o);o.hwndOwner=gWnd;o.lpstrFile=f;o.nMaxFile=MAX_PATH;o.lpstrFilter="Tiny project\0*.tinyproj\0All files\0*.*\0\0";o.Flags=OFN_FILEMUSTEXIST;if(!GetOpenFileNameA(&o))return;project=widen(f);load();}
static void add(EntityKind k){Entity e;e.id=g.scene().nextId++;e.kind=k;e.name=kindName(k);int n=1;for(auto&q:g.scene().entities)if(q.kind==k)++n;e.name+=std::to_string(n);if(k==EntityKind::Plane)e.transform.scale={5,.1f,5};if(k==EntityKind::Cube){e.transform.position={0,1,0};e.dynamic=true;}if(k==EntityKind::Sphere)e.transform.position={2,1,0};if(k==EntityKind::PointLight)e.transform.position={2,3,-2};if(k==EntityKind::SpotLight)e.transform.position={-2,3,-2};g.scene().entities.push_back(e);sel=(int)g.scene().entities.size()-1;refresh();out(e.name+" added.");}
static float gf(HWND h,float old){char b[64];GetWindowTextA(h,b,64);char*e;float v=strtof(b,&e);return e==b?old:v;}static void apply(){if(sel<0)return;auto&e=g.scene().entities[sel];char n[128];GetWindowTextA(gName,n,128);if(n[0])e.name=n;e.transform.position={gf(gX,e.transform.position.x),gf(gY,e.transform.position.y),gf(gZ,e.transform.position.z)};e.transform.rotation={gf(gRX,e.transform.rotation.x),gf(gRY,e.transform.rotation.y),gf(gRZ,e.transform.rotation.z)};e.transform.scale={std::max(.01f,gf(gSX,e.transform.scale.x)),std::max(.01f,gf(gSY,e.transform.scale.y)),std::max(.01f,gf(gSZ,e.transform.scale.z))};refresh();out("Inspector applied.");}
static void build(){if(project.empty()){out("Open or create a project first.");return;}save();fs::path dir=fs::path(project).parent_path()/"build";fs::create_directories(dir);wchar_t exe[MAX_PATH];GetModuleFileNameW(nullptr,exe,MAX_PATH);fs::path runtime=fs::path(exe).parent_path()/L"TinyAIRuntime.exe";if(!fs::exists(runtime)){out("Runtime executable is missing. Build the engine first.");return;}fs::copy_file(runtime,dir/"Game.exe",fs::copy_options::overwrite_existing);fs::copy_file(project,dir/"game.tinyproj",fs::copy_options::overwrite_existing);out("Built standalone game in "+narrow(dir.wstring()));}
static LRESULT CALLBACK proc(HWND h,UINT m,WPARAM w,LPARAM l){if(m==WM_SIZE){if(g.graphics().ready())g.resize(LOWORD(l)-330,HIWORD(l)-170);return 0;}if(m==WM_COMMAND){int id=LOWORD(w);if(id==NEWP)newProject();else if(id==OPENP)openProject();else if(id==SAVE)save();else if(id==BUILD)build();else if(id==PLAY){g.play(true);out("Play mode running. WASD moves dynamic objects.");}else if(id==STOP){g.play(false);out("Play mode stopped.");}else if(id==ADD_CUBE)add(EntityKind::Cube);else if(id==ADD_SPHERE)add(EntityKind::Sphere);else if(id==ADD_PLANE)add(EntityKind::Plane);else if(id==ADD_CAM)add(EntityKind::Camera);else if(id==ADD_DIR)add(EntityKind::DirectionalLight);else if(id==ADD_POINT)add(EntityKind::PointLight);else if(id==ADD_SPOT)add(EntityKind::SpotLight);else if(id==DEL&&sel>=0){if(g.scene().entities[sel].id!=g.scene().cameraId){g.scene().entities.erase(g.scene().entities.begin()+sel);sel=-1;refresh();out("Deleted.");}}else if(id==DUP&&sel>=0){auto e=g.scene().entities[sel];e.id=g.scene().nextId++;e.name+=" Copy";e.transform.position.x+=1;g.scene().entities.push_back(e);sel=(int)g.scene().entities.size()-1;refresh();}else if(id==APPLY)apply();else if(id==FOCUS&&sel>=0){auto&e=g.scene().entities[sel];if(e.kind!=EntityKind::Camera){auto c=std::find_if(g.scene().entities.begin(),g.scene().entities.end(),[](auto&q){return q.kind==EntityKind::Camera;});if(c!=g.scene().entities.end()){c->transform.position={e.transform.position.x,e.transform.position.y+2,e.transform.position.z-6};out("Focused selection.");}}}return 0;}if(m==WM_LBUTTONDOWN&&LOWORD(l)>220&&HIWORD(l)>70){int x=LOWORD(l)-220,y=HIWORD(l)-70;RECT r{0,0,1,1};int w=0;GetClientRect(h,&r);POINT p{LOWORD(l),HIWORD(l)};for(int i=0;i<(int)g.scene().entities.size();++i){auto&e=g.scene().entities[i];if(e.kind==EntityKind::Camera)continue;float sx=220+(r.right-330)/2.0f+e.transform.position.x*35;float sy=70+(r.bottom-170)/2.0f-e.transform.position.y*35;if((p.x-sx)*(p.x-sx)+(p.y-sy)*(p.y-sy)<900){sel=i;refresh();break;}}return 0;}if(m==WM_DESTROY){PostQuitMessage(0);return 0;}return DefWindowProcA(h,m,w,l);}
int WINAPI WinMain(HINSTANCE hi,HINSTANCE,HINSTANCE, int); // dummy forward intentionally unused
int WINAPI WinMain(HINSTANCE hi,HINSTANCE,LPSTR,int){WNDCLASSA wc{};wc.lpfnWndProc=proc;wc.hInstance=hi;wc.lpszClassName="TinyEditor";wc.hCursor=LoadCursor(nullptr,IDC_ARROW);RegisterClassA(&wc);gWnd=CreateWindowA("TinyEditor","Tiny AI Game Engine",WS_OVERLAPPEDWINDOW|WS_VISIBLE,80,50,1200,800,nullptr,nullptr,hi,nullptr);g.initialize(gWnd,870,630);CreateWindowA("STATIC","SCENE",WS_CHILD|WS_VISIBLE,10,45,200,24,gWnd,nullptr,hi,nullptr);gTree=CreateWindowA("LISTBOX","",WS_CHILD|WS_VISIBLE|WS_BORDER|LBS_NOTIFY,10,70,200,420,gWnd,(HMENU)200,hi,nullptr);button(gWnd,"New",NEWP,10,5,55);button(gWnd,"Open",OPENP,70,5,55);button(gWnd,"Save",SAVE,130,5,55);button(gWnd,"Build",BUILD,190,5,65);button(gWnd,"Play",PLAY,265,5,55);button(gWnd,"Stop",STOP,325,5,55);button(gWnd,"Cube",ADD_CUBE,10,500,55);button(gWnd,"Sphere",ADD_SPHERE,70,500,65);button(gWnd,"Plane",ADD_PLANE,140,500,60);button(gWnd,"Camera",ADD_CAM,205,500,65);button(gWnd,"Dir Light",ADD_DIR,275,500,70);button(gWnd,"Point",ADD_POINT,350,500,55);button(gWnd,"Spot",ADD_SPOT,410,500,55);button(gWnd,"Delete",DEL,10,535,65);button(gWnd,"Duplicate",DUP,80,535,70);button(gWnd,"Focus",FOCUS,155,535,55);CreateWindowA("STATIC","INSPECTOR",WS_CHILD|WS_VISIBLE,930,45,180,24,gWnd,nullptr,hi,nullptr);label(gWnd,"Name",930,75);gName=edit(gWnd,"",980,72,180,24);const char*names[]={"X","Y","Z","RX","RY","RZ","SX","SY","SZ"};HWND*es[]={&gX,&gY,&gZ,&gRX,&gRY,&gRZ,&gSX,&gSY,&gSZ};for(int i=0;i<9;++i){int row=i/3,col=i%3;label(gWnd,names[i],930+col*75,110+row*55);*es[i]=edit(gWnd,"0",950+col*75,108+row*55,65,24);}button(gWnd,"Apply",APPLY,930,280,70);gOut=CreateWindowA("EDIT","Ready.",WS_CHILD|WS_VISIBLE|WS_BORDER|ES_MULTILINE|ES_READONLY,220,650,940,70,gWnd,nullptr,hi,nullptr);refresh();MSG msg;while(GetMessageA(&msg,nullptr,0,0)){TranslateMessage(&msg);DispatchMessageA(&msg);if(g.playing())g.tick(.016f);g.render();}return 0;}
