# Runtime

Files required by exported Tiny AI Game Engine games.

The release builder will copy required runtime DLLs beside exported game executables so players do not need to install a compiler or runtime package separately.